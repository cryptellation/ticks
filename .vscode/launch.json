{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Worker",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/cmd/worker",
			"args": ["serve"],
            "env": {},
			"preLaunchTask": "Start dependencies",
            "postDebugTask": "Stop dependencies"
        }
    ]
}